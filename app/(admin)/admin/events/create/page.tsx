import CreateEvent from "@/components/admin/CreateEvent";

export const metadata = {
  title: "Create Event - KoloKwa Admin",
  description: "Create a new event for KoloKwa.",
};

export default function CreateEventPage() {
  return <CreateEvent />;
}
